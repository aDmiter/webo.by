services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: webo_by
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/data

volumes:
  mysql_data:
